tell application "System Events"
    -- Use the Spotlight keyboard shortcut (Command + Space)
    key code 49 using {command down}
    delay 0.5 -- Wait half a second for Spotlight to open
    keystroke "PLACEHOLDER"
    delay 1
    keystroke "\r"
end tell
